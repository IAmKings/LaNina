# B/C 组上线：staging Worker 部署 + Access 三值 secrets 注入

## Goal

B 组已完成 staging Worker 部署（worker填入 D1/RAW binding，Access 三值成功注入为 secrets，staging domain enso-monitor-staging.<YOUR-SUBDOMAIN>.workers.dev 上线），C 组 Access 在 Cloudflare 面板手动建立 + AUD/Issuer/JWKS 三值回填；D 组待发出一页纸。

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
